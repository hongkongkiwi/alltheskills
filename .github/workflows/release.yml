name: Release

on:
  release:
    types: [created]

jobs:
  build-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            artifact_suffix: -x86_64-unknown-linux-gnu
          - os: macos-latest
            artifact_suffix: -x86_64-apple-darwin
          - os: windows-latest
            artifact_suffix: -x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: rustup default stable

      - name: Build release
        run: cargo build --release --package alltheskills-cli

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: alltheskills-cli${{ matrix.artifact_suffix }}
          path: target/release/alltheskills
          if-no-files-found: error

  create-github-release:
    needs: build-release
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
